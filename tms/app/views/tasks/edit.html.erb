<h1><%= t("page.task.title.edit") %></h1>

<%= render 'form', task: @task %>

<%= link_to "#{t("page.task.link.detail")}".html_safe, @task %> |
<%= link_to "#{t("page.task.link.back")}".html_safe, tasks_path %>
