<% provide(:title, 'Task Edit') %>
<h1>Task Update Form</h1>
<div class="row">
  <%= form_for @task, url: task_path(@task.id) do |f| %>
    <%= render "form", { f: f } %>
  <% end %>
  <%= link_to tasks_path do %>
  <span class="glyphicon glyphicon-menu-left"></span><%= t('header.back') %>
<% end %>
</div>
