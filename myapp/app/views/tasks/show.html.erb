<h1>タスク詳細</h1>

<h2><%= @task.title %></h2>
<p><%= @task.memo %></p>

<%= link_to 'Back', '/tasks' %>
<%= link_to 'Edit', edit_task_path(@task) %>
<%= link_to 'delete', task_path(@task), method: :delete, data: { confirm: '消しますか？' } %>
