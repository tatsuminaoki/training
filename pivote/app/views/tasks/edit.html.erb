<h1><%= t('headline.task.edit') %></h1>

<div class="nav justify-content-end">
  <%= link_to t('link.all'), tasks_path, class: 'nav-link' %>
</div>

<%= render partial: 'form', locals: {task: @task} %>
