<p id="notice"><%= notice %></p>

<p>
  <strong><%= Task.human_attribute_name(:title) %></strong>
  <%= @task.title %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:body) %></strong>
  <%= @task.body %>
</p>

<%= link_to t('edit'), edit_task_path(@task) %> |
<%= link_to t('back'), tasks_path %>
