<!DOCTYPE html>
<html>
  <head>
    <title>TrainingApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag 'application' %>
    <%= stylesheet_pack_tag 'application' %>
  </head>

  <body class="bg-light">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <%= sign_out %>
          <%= link_to t('admins'), admin_users_path %>
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
