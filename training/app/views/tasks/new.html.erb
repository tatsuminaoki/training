<div>
  <h1>タスク作成</h1>
</div>
<%= form_for(@task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br/>

  <%= f.label :description %>
  <%= f.text_area :description %>
  <br/>

  <%= f.submit 'タスク作成', data: { disable_with: '作成中...'} %>
<% end %>
