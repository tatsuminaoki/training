<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Tu~du~", logged_in? ? root_path : login_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t('header.task_management') %><b class="caret"></b>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to t('header.task_index'), root_path %>
              </li>
              <li>
                <%= link_to t('header.task_new'), new_task_path %>
              </li>
            </ul>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t('header.label_management') %><b class="caret"></b>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to t('header.label_index'), labels_path %>
              </li>
              <li>
                <%= link_to t('header.label_new'), new_label_path %>
              </li>
            </ul>
          </li>
          <li>
            <%= link_to t('header.logout'), logout_path, method: :delete %>
          </li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
