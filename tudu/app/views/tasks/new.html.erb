<h1><%= t('task.new.title') %></h1>
<div>
  <%= form_for(@task) do |f| %>
    <div>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <div>
      <%= f.label :content %>
      <%= f.text_area :content %>
    </div>
    <div>
      <%= f.submit t('action.create') %>
    </div>
  <% end %>
</div>
