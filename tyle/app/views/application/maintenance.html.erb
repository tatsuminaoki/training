<div class="card col-md-9">
  <div class="card-body">
    <h1><%= t('message.maintenance_title') %></h1>
    <p class='alert alert-danger'><%= t('message.maintenance_period') + ': ' + @period %></p>
    <p><%= t('message.maintenance_comment') %></p>
  </div>
</div>
