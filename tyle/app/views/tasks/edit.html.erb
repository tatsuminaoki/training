<div>
  <h1>TYLE</h1>
  <%= link_to t('button.index'), tasks_path %>
  <%= link_to t('button.add'), new_task_path %>
</div>

<hr>

<caption><%= t('operation.task-edit') %></caption>

<%= render 'form', { task: @task }%>
