<h1>All Tasks</h1>

<% if flash[:success] %>
  <p><%= flash[:success] %></p>
<% end %>

<%= link_to 'Create New Task', tasks_new_path %>

<% if @tasks.any? %>
<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, tasks_show_path(task) %></li>
  <% end %>
</ul>
<% end %>